[[production]]
name = "not the entry"
type = "pure"
replacement = "replacement"
terminal = "terminal"

[[production]]
name = "range production"
type = "range"
lower_bound = -10
upper_bound = 10

[[production]]
name = "janet production"
type = "janet"
replacement = '''#replacement script
(defn print-greeting
  [greetee]
  (print "replacement, " greetee "!"))

(defn main
  [& args]
  (pp args) # pretty-prints the args tuple
  (print (length args))
  (print-greeting (get args 1)))
'''
terminal = '''#terminal script
(defn print-greeting
  [greetee]
  (print "terminal, " greetee "!"))

(defn main
  [& args]
  (pp args) # pretty-prints the args tuple
  (print (length args))
  (print-greeting (get args 1)))
'''